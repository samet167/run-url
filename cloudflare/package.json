{
  "name": "render-keepalive-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Render 24/7 Keep-Alive Hub with Native Edge Cron Triggers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.4"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
